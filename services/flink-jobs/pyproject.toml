[project]
name = "logivision-flink-jobs"
version = "0.1.0"
requires-python = ">=3.11,<3.12"

dependencies = [
    "apache-flink==1.18.1",
    "fastavro>=1.9.0,<2.0.0",
    "confluent-kafka>=2.5.0,<3",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
